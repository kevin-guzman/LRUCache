{
  "name": "tech_test",
  "version": "1.0.0",
  "description": "An LRUCache (Least Recently Used Cache) is a fixed item size object cache. To add or update an object in the cache you can use the `set` method:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
